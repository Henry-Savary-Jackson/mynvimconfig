local options = {
  ensure_installed = { "lua", "java", "python", "http", "json" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
